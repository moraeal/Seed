import { ArrowRight, BarChart3, Clapperboard, Users } from "lucide-react";
import { AUDITION_URL } from "../data/homeData";
export default function AdBanner() {
  const ads = [{ icon: Clapperboard, tag: "SEED PROJECT", title: "쇼츠 시민 제안 공개 오디션", body: "쇼츠 영상 하나로 시민운동을 시작할 수 있습니다. 생활 속에서 꼭 고쳐졌으면 하는 일을 제안하고, 동료시민의 공감과 질문으로 시민실험을 키워갑니다.", button: "공개오디션 보러가기", href: AUDITION_URL }, { icon: BarChart3, tag: "PARTNER WANTED", title: "공익감시 데이터랩 파트너를 찾습니다", body: "공익기관, 위원회, 재단, 정책사업의 예산과 성과를 시민의 언어로 분석할 데이터 파트너를 모집합니다.", button: "데이터랩 참여하기", href: undefined }];
  return <section className="bg-paper pb-16 sm:pb-20"><div className="container-page grid gap-4 lg:grid-cols-2">{ads.map((ad, index) => <div key={ad.title} className={`relative overflow-hidden rounded-lg p-7 sm:p-9 ${index ? "bg-[#E5EAF0]" : "bg-[#EEE5CC]"}`}><ad.icon className="absolute -bottom-5 right-6 size-36 text-navy/[0.08]" strokeWidth={1.2} /><span className="text-[11px] font-extrabold tracking-[0.15em] text-green-mid">{ad.tag}</span><h3 className="relative mt-3 max-w-md text-xl font-extrabold text-navy">{ad.title}</h3><p className="relative mt-3 max-w-md text-sm leading-6 text-charcoal/60">{ad.body}</p><a href={ad.href ?? "#"} target={ad.href ? "_blank" : undefined} rel={ad.href ? "noopener noreferrer" : undefined} className="relative mt-6 inline-flex min-h-11 items-center gap-2 text-sm font-bold text-green-deep">{ad.button}<ArrowRight size={16} /></a>{index === 0 && <Users className="absolute right-16 top-7 text-gold/40" size={38} />}</div>)}</div></section>;
}
