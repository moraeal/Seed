import { ArrowRight, BarChart3, Clapperboard, Users } from "lucide-react";
export default function AdBanner() {
  const ads = [{ icon: Clapperboard, tag: "COMING SOON", title: "시민운동 프로젝트 공개 오디션 준비중", body: "2분 제안 영상으로 시민운동 아이디어를 올리고, 시민들의 공개 평가를 통해 실제 시민실험으로 연결합니다.", button: "오디션 알림 신청" }, { icon: BarChart3, tag: "PARTNER WANTED", title: "공익감시 데이터랩 파트너를 찾습니다", body: "공익기관, 위원회, 재단, 정책사업의 예산과 성과를 시민의 언어로 분석할 데이터 파트너를 모집합니다.", button: "데이터랩 참여하기" }];
  return <section className="bg-paper pb-16 sm:pb-20"><div className="container-page grid gap-4 lg:grid-cols-2">{ads.map((ad, index) => <div key={ad.title} className={`relative overflow-hidden rounded-lg p-7 sm:p-9 ${index ? "bg-[#E5EAF0]" : "bg-[#EEE5CC]"}`}><ad.icon className="absolute -bottom-5 right-6 size-36 text-navy/[0.08]" strokeWidth={1.2} /><span className="text-[11px] font-extrabold tracking-[0.15em] text-green-mid">{ad.tag}</span><h3 className="relative mt-3 max-w-md text-xl font-extrabold text-navy">{ad.title}</h3><p className="relative mt-3 max-w-md text-sm leading-6 text-charcoal/60">{ad.body}</p><a href="#" className="relative mt-6 inline-flex items-center gap-2 text-sm font-bold text-green-deep">{ad.button}<ArrowRight size={16} /></a>{index === 0 && <Users className="absolute right-16 top-7 text-gold/40" size={38} />}</div>)}</div></section>;
}
