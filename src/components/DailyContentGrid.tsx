import { BarChart2, Flame, Newspaper, Vote } from "lucide-react";
import { useState } from "react";
import { AUDITION_URL, dailyVotes, discussions, popularNews, todayBriefings } from "../data/homeData";
export default function DailyContentGrid() {
  const [choices, setChoices] = useState<Record<number, number>>({});
  const lists = [{ icon: Flame, title: "뜨거운 토론", items: discussions }, { icon: Newspaper, title: "인기뉴스", items: popularNews }, { icon: BarChart2, title: "오늘의 브리핑", items: todayBriefings }];
  return <section className="section-band bg-[#F4F5F2]"><div className="container-page"><span className="section-kicker">DAILY CIVIC AGENDA</span><h2 className="mt-3 text-3xl font-extrabold text-navy">오늘 씨앗연대에서 함께 볼 의제</h2><div className="mt-9 grid gap-4 lg:grid-cols-4"><div className="rounded-lg bg-green-deep p-6 text-white"><h3 className="flex items-center gap-2 text-lg font-extrabold"><Vote className="text-gold" size={21} />시민투표</h3><div className="mt-5 space-y-6">{dailyVotes.map((vote, voteIndex) => { const audition = vote.question.includes("공개 오디션"); return <div key={vote.question}><p className="text-sm font-bold leading-6">{vote.question}</p><div className="mt-3 flex flex-wrap gap-1.5">{vote.options.map((option, optionIndex) => <button key={option} onClick={() => setChoices({ ...choices, [voteIndex]: optionIndex })} className={`rounded-full border px-2.5 py-1.5 text-[10px] font-semibold transition ${choices[voteIndex] === optionIndex ? "border-gold bg-gold text-navy" : "border-white/20 text-white/70 hover:border-white/50"}`}>{option}</button>)}</div>{audition && <a href={AUDITION_URL} target="_blank" rel="noopener noreferrer" className="mt-3 inline-flex min-h-8 items-center text-xs font-bold text-gold">공개오디션 보기 →</a>}</div>; })}</div></div>{lists.map((list) => <div key={list.title} className="rounded-lg border border-green-deep/10 bg-paper p-6"><h3 className="flex items-center gap-2 text-lg font-extrabold text-navy"><list.icon className="text-green-mid" size={21} />{list.title}</h3><ol className="mt-4 divide-y divide-green-deep/10">{list.items.map((item, index) => { const audition = item.includes("공개 오디션") || item.includes("쇼츠 제안"); return <li key={item}><a href={audition ? AUDITION_URL : "#"} target={audition ? "_blank" : undefined} rel={audition ? "noopener noreferrer" : undefined} className="group flex gap-3 py-4 text-sm font-semibold leading-6 text-charcoal/70 hover:text-green-deep"><span className="text-xs font-bold text-gold">0{index + 1}</span>{item}</a></li>; })}</ol></div>)}</div></div></section>;
}
