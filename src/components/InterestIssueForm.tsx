import { ArrowDown, CheckCircle2, Send } from "lucide-react";
import type { FormEvent } from "react";
const steps = ["이슈 등록", "운영진 검토", "시민브리핑 작성", "씨앗공론장 개설", "시민실험·캠페인 연결"];
export default function InterestIssueForm() {
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); window.alert("관심이슈가 등록되었습니다. 데모에서는 실제 저장되지 않습니다."); event.currentTarget.reset(); };
  return <section className="section-band bg-ivory"><div className="container-page"><div className="mb-9 max-w-3xl"><span className="section-kicker">YOUR QUESTION MATTERS</span><h2 className="mt-3 text-3xl font-extrabold text-navy">씨드 관심이슈를 등록해보세요</h2><p className="mt-4 text-sm leading-7 text-charcoal/70">사회문제, 생활 속 불편, 공익기관에 대한 질문을 등록해보세요. 시민브리핑과 공론장으로 연결하겠습니다.</p></div><div className="grid overflow-hidden rounded-lg border border-green-deep/10 bg-paper shadow-soft lg:grid-cols-[1.35fr_0.65fr]"><form onSubmit={submit} className="grid gap-4 p-6 sm:grid-cols-2 sm:p-9"><label className="field sm:col-span-2"><span>관심이슈 제목</span><input required placeholder="어떤 문제를 함께 살펴볼까요?" /></label><label className="field sm:col-span-2"><span>왜 이 이슈가 중요하다고 생각하시나요?</span><textarea required rows={4} placeholder="생활 속 경험이나 문제의식을 들려주세요." /></label><label className="field sm:col-span-2"><span>관련 기사 또는 자료 링크</span><input type="url" placeholder="https://" /></label><label className="field"><span>이름 또는 닉네임</span><input required placeholder="씨앗시민" /></label><label className="field"><span>이메일</span><input required type="email" placeholder="name@example.com" /></label><button className="button-primary mt-2 sm:col-span-2" type="submit"><Send size={16} />관심이슈 등록하기</button></form><aside className="bg-green-pale p-7 sm:p-9"><h3 className="text-lg font-extrabold text-green-deep">관심이슈는 이렇게 자랍니다</h3><div className="mt-7">{steps.map((step, index) => <div key={step} className="flex gap-3"><div className="flex flex-col items-center"><CheckCircle2 className="text-green-mid" size={21} />{index < steps.length - 1 && <ArrowDown className="my-2 text-green-deep/25" size={15} />}</div><p className="pb-5 text-sm font-bold text-charcoal/75"><span className="mr-2 text-gold">0{index + 1}</span>{step}</p></div>)}</div><p className="mt-3 border-t border-green-deep/10 pt-5 text-xs leading-6 text-charcoal/60">등록된 내용은 운영진 검토 후 공개됩니다. 이 화면은 데모로 실제 저장되지 않습니다.</p></aside></div></div></section>;
}
