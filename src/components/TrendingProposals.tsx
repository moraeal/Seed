import { ArrowUpRight, MessageSquare } from "lucide-react";
import { trendingProposals } from "../data/homeData";
export default function TrendingProposals() {
  const cardColors = ["#F1F6F2", "#F2F5F8", "#F8F4EA"];

  return <section className="section-band"><div className="container-page"><div className="section-heading"><div><span className="section-kicker">CIVIC PROPOSALS</span><h2>지금 뜨는 시민제안</h2><p>시민의 작은 문제의식이 씨앗운동의 출발점입니다. 지금 동료시민들이 던지고 있는 질문을 확인해보세요.</p></div><a href="#" className="text-link">시민제안 전체보기 <ArrowUpRight size={16} /></a></div><div className="mt-9 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{trendingProposals.map((item, index) => <article key={String(item.title)} className="content-card flex min-h-64 flex-col border-green-deep/15" style={{ backgroundColor: cardColors[index % cardColors.length] }}><span className="w-fit rounded-full bg-green-pale px-3 py-1 text-xs font-bold text-green-deep">{item.label}</span><h3 className="mt-5 text-lg font-extrabold leading-7 text-navy">{item.title}</h3><p className="mt-3 flex-1 text-sm leading-6 text-charcoal/60">{item.description}</p><div className="mt-5 flex items-center justify-between border-t border-green-deep/10 pt-4 text-xs"><span className="flex items-center gap-1.5 text-charcoal/50"><MessageSquare size={14} />의견 {item.comments}개</span><a href={item.href ?? "#"} target={item.href ? "_blank" : undefined} rel={item.href ? "noopener noreferrer" : undefined} className="font-bold text-green-deep">자세히 보기 →</a></div></article>)}</div></div></section>;
}
