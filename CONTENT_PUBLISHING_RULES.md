# SEED VOICE 콘텐츠 게시 운영 원칙

이 문서는 씨앗의 소리 콘텐츠 작업이 보안·인증·배포 구조와 불필요하게 얽히지 않도록 하기 위한 고정 운영 기준이다.

## 1. 콘텐츠 작업의 기본 범위

새 칼럼·브리핑·뉴스를 게시할 때 원칙적으로 다음 파일만 수정한다.

1. `src/data/columns/` 또는 해당 콘텐츠 유형의 데이터 폴더 안의 신규 콘텐츠 파일
2. 해당 콘텐츠를 목록에 연결하는 데이터 인덱스 파일
3. 필요한 이미지 파일
4. 영문 번역 또는 소셜 이미지 파일이 실제로 필요한 경우에만 해당 파일

콘텐츠 게시만을 이유로 인증·회원·보안·데이터베이스·배포 설정을 수정하지 않는다.

## 2. 보호 영역

일반 콘텐츠 작업에서는 아래 영역을 변경하지 않는다.

- `src/auth.tsx`
- `src/lib/turnstile.ts`
- `src/lib/engagement.ts`
- `supabase/`
- `.github/workflows/`
- 회원가입·로그인·뉴스레터 보안 관련 코드
- 백업 관련 설정

위 영역의 변경이 필요하면 콘텐츠 작업과 분리된 별도 시스템 작업으로 취급한다.

## 3. 신규 칼럼 게시 방식

신규 칼럼은 기존 방식대로 `src/data/columns/` 안에 독립 파일로 작성한다.

예:

`src/data/columns/exampleColumn.ts`

그리고 `src/data/columns.ts`에서 직접 import한 뒤 `columns` 배열 맨 앞에 연결한다.

불필요한 registry, 중간 index, 임시 route, 중복 데이터 파일을 만들지 않는다.

## 4. 기존 콘텐츠 보존

`src/data/columnsLegacy.ts`는 기존 콘텐츠 보존용이다.

새 콘텐츠를 게시할 때 이 파일을 수정하지 않는다. 기존 글의 수정 요청이 있을 때만 해당 항목을 직접 수정한다.

## 5. 이미지 원칙

- 대표 이미지와 본문 이미지의 중복 노출 여부를 확인한다.
- 대표 이미지 경로는 실제 존재하는 파일만 사용한다.
- 임시 placeholder, base64 백업, marker 파일을 저장소에 남기지 않는다.
- 소셜 공유 이미지는 기존 자동 생성 흐름을 우선 사용한다.

## 6. 배포 전 확인

콘텐츠 게시 후 반드시 다음을 확인한다.

- 빌드 성공
- GitHub Pages deploy 성공
- 콘텐츠 목록 노출
- 상세 페이지 정상 열림
- 대표 이미지 정상 표시
- 기존 콘텐츠 영향 없음

## 7. 변경 최소화 원칙

콘텐츠 1편 게시를 위해 시스템 전체를 리팩터링하지 않는다.

기본 목표는 다음과 같다.

**콘텐츠 파일 1개 + 목록 연결 최소 수정 + 필요한 이미지**

구조 개선이 필요하더라도 콘텐츠 게시와 같은 작업에서 동시에 진행하지 않는다.

## 8. 복구 기준점

2026-09-08 콘텐츠 구조 단순화 직전 운영 상태는 다음 복구 브랜치에 보존되어 있다.

`recovery/pre-simplify-2026-09-08`

## 9. 독자용 용어와 이메일 발송 범위

- ‘대표기사’와 ‘대표이미지’는 내부 소통 용어다. 독자용 본문·버튼·캡션·대체 텍스트·메타데이터·이메일에는 노출하지 않는다.
- 깊게 읽기에서 짧은 글로 돌아가는 링크는 ‘5분 요약본 보기’로 표시한다. 영어판은 ‘Read the 5-minute summary’를 사용한다.
- 깊게 읽기는 홈페이지에서 연결하는 보충기사이며 이메일로 별도 발송하지 않는다. 한 주제의 짧은 글과 깊게 읽기를 두 통으로 보내지 않는다.
- 씨앗언어의 이메일 발송 대상은 `newsletterEligible === true`로 명시된 글만 선택한다. `false` 또는 미지정이면 발송 대상에서 제외한다. 기존 글에 표시가 추가됐다는 이유만으로 과거 글을 소급 발송하지 않는다.
- 향후 발송 시스템은 최초 정식 게시 후 3시간에 최신 게시본으로 보내고, 같은 글은 수정·재배포되어도 구독자별로 한 번만 보낸다. 이 문서와 대상 표시는 발송 시스템을 활성화하거나 예약하지 않는다.

콘텐츠 구조 변경으로 문제가 발생하면 이 브랜치를 비교 기준으로 사용한다.
